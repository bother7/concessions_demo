<%= form_with(model: game, local: true) do |form| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date, id: :game_date %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :game_title %>
  </div>

  <div class="field">
    <%= form.label :food %>
    <%= form.check_box :food, id: :game_food %>
  </div>

  <div class="field">
    <%= form.label :drinks %>
    <%= form.check_box :drinks, id: :game_drinks %>
  </div>

  <div class="field">
    <%= form.label :snacks %>
    <%= form.check_box :snacks, id: :game_snacks %>
  </div>

  <div class="field">
    <%= form.label :alcohol %>
    <%= form.check_box :alcohol, id: :game_alcohol %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
